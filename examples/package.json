{
  "name": "slack-notify",
  "version": "1.0.0",
  "description": "Notify on slack channel when package versions invalidate rules",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "cli",
    "script",
    "slack",
    "notify",
    "rules"
  ],
  "license": "ISC",
  "dependencies": {
    "@slack/web-api": "^6.7.2",
    "pkg-version-report-gen": "^1.1.6"
  }
}
